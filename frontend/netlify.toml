[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_JOURNEYS_BASE_URL = "https://southeastern.stage.otrl.io"

# SPA fallback — client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
